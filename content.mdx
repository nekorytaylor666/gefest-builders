# Структура програмы

**Лекция: Структура программ в JavaScript**

**1. Введение**

Представьте, что вы строите дом. Ваш дом — это программа на JavaScript, а инструменты и материалы, которые вы используете, — это переменные, функции, циклы и условия.

**2. Переменные**

Переменные — это коробки в вашем доме, в которых вы храните разные вещи.

- **Объявление переменных:** Создание коробки.

  ```jsx
  let имя = значение;
  ```

- **Пример:** Создаем коробку с именем `age` и кладем в нее значение `25`.

  ```jsx
  let age = 25;
  ```

+++

**3. Функции**

Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

  Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

  Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

  Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

  Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

<CodeEditor expectedOutput={"hello world!\n"} />

- **Пример:** Рецепт приготовления смузи.

+++

**3. Функции**

Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

<CodeEditor expectedOutput={"console.log('hello world!');"} />

- **Пример:** Рецепт приготовления смузи.

+++

**3. Функции**

Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

<Quiz
  question="Пример вопроса"
  options={["Ответ 1", "Ответ 2", "Ответ 3"]}
  correctAnswer="Ответ 1"
/>

- **Пример:** Рецепт приготовления смузи.

+++

**3. Функции**

Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

<CodeEditor expectedOutput={"console.log('hello world!');"} />

- **Пример:** Рецепт приготовления смузи.

+++

**3. Функции**

Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

<Next />

- **Пример:** Рецепт приготовления смузи.

+++

**3. Функции**

Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

<CodeEditor expectedOutput={"console.log('hello world!');"} />

- **Пример:** Рецепт приготовления смузи.

+++

**3. Функции**

Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

<CodeEditor expectedOutput={"console.log('hello world!');"} />

- **Пример:** Рецепт приготовления смузи.

+++

**3. Функции**

Функции — это рецепты приготовления блюд. Вы можете следовать рецепту (вызывать функцию) каждый раз, когда хотите приготовить блюдо (получить результат).

- **Объявление функции:**

  ```jsx
  function имя(параметры) {
    // тело функции
    return результат;
  }
  ```

<CodeEditor expectedOutput={"console.log('hello world!');"} />

- **Пример:** Рецепт приготовления смузи.
